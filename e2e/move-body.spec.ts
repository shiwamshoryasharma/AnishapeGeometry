import {test,expect} from '@playwright/test'
test('Move body offers three arrows, Tab values, cancel, commit and saved history',async({page},info)=>{await page.goto('/#projects');await page.getByRole('button',{name:'New project',exact:true}).click();await page.getByRole('dialog').getByRole('button',{name:'Create project',exact:true}).click();await page.getByRole('button',{name:'Create sketch',exact:true}).click();await page.getByRole('button',{name:'Sketch on XY plane',exact:true}).click();await page.getByRole('button',{name:'Finish sketch',exact:true}).click();await page.getByRole('button',{name:'Extrude',exact:true}).click();await page.getByRole('button',{name:'Apply',exact:true}).click();await expect(page.getByTestId('model-volume')).toHaveText('24000.000 mm³');await page.getByRole('button',{name:'Extrude',exact:true}).click();await page.getByRole('button',{name:'Apply',exact:true}).click();await expect(page.getByTestId('model-volume')).toHaveText('48000.000 mm³');await page.getByRole('button',{name:'Select body Extrude 2',exact:true}).click();await page.getByRole('toolbar',{name:'Modeling tools'}).getByRole('button',{name:'Move body',exact:true}).click();const target=page.getByLabel('Target body',{exact:true});await expect(target.locator('option:checked')).toHaveText('Extrude 2');for(const axis of ['X','Y','Z'])await expect(page.getByRole('button',{name:'Drag move '+axis,exact:true})).toBeVisible();const handle=page.getByRole('button',{name:'Drag move X',exact:true}),b=await handle.boundingBox();await page.mouse.move(b!.x+b!.width/2,b!.y+b!.height/2);await page.mouse.down();await page.mouse.move(b!.x+b!.width/2+24,b!.y+b!.height/2,{steps:4});await page.keyboard.press('Tab');const input=page.getByRole('textbox',{name:'Canvas move X',exact:true});await expect(input).toBeFocused();await input.fill('70');await page.mouse.up();await input.press('Enter');await expect(page.getByText('FEATURE PREVIEW',{exact:true})).toBeVisible();await page.getByLabel('Move Y',{exact:true}).fill('-15');await page.getByLabel('Move Z',{exact:true}).fill('5');await page.getByRole('button',{name:'Preview geometry',exact:true}).click();await expect(page.getByText('FEATURE PREVIEW',{exact:true})).toBeVisible();await page.screenshot({path:info.outputPath('move-body-triad.png')});await page.getByRole('button',{name:'Apply',exact:true}).click();await expect(page.getByRole('button',{name:'Move body 1',exact:true})).toBeVisible();await expect(page.getByTestId('model-volume')).toHaveText('48000.000 mm³');await expect(page.locator('.bound-value').first()).toContainText('120.000');await page.reload();await expect(page.getByRole('button',{name:'Move body 1',exact:true})).toBeVisible();await page.getByRole('button',{name:'Move body 1',exact:true}).click();await expect(page.getByLabel('Move X',{exact:true})).toHaveValue('70');await page.getByLabel('Move X',{exact:true}).fill('100');await page.getByRole('button',{name:'Cancel',exact:true}).click();await expect(page.locator('.bound-value').first()).toContainText('120.000');await page.getByRole('button',{name:'Undo',exact:true}).click();await expect(page.locator('.bound-value').first()).toContainText('50.000');await page.getByRole('button',{name:'Redo',exact:true}).click();await expect(page.locator('.bound-value').first()).toContainText('120.000')})
test('cube axis labels stay separate at narrow viewing angles',async({page})=>{await page.goto('/#projects');await page.getByRole('button',{name:'New project',exact:true}).click();await page.getByRole('dialog').getByRole('button',{name:'Create project',exact:true}).click();const cube=page.getByLabel('Drag view cube to rotate',{exact:true}),b=await cube.boundingBox();await page.mouse.move(b!.x+b!.width/2,b!.y+b!.height/2);await page.mouse.down();for(let i=1;i<=20;i++){await page.mouse.move(b!.x+b!.width/2+i*4,b!.y+b!.height/2+i*2);const boxes=await cube.locator('text:visible').evaluateAll(nodes=>nodes.map(n=>{const b=n.getBoundingClientRect();return {x:b.x,y:b.y,r:b.right,b:b.bottom}}));for(let j=0;j<boxes.length;j++)for(let k=j+1;k<boxes.length;k++){const a=boxes[j],b=boxes[k];expect(a.r<=b.x||b.r<=a.x||a.b<=b.y||b.b<=a.y).toBe(true)}}await page.mouse.up()})
