import {test,expect} from '@playwright/test'
test('Save As selects a .anishape file and editor Open restores solid history',async({page})=>{
 await page.addInitScript(()=>{Object.defineProperty(window,'showSaveFilePicker',{value:async(options:unknown)=>{(window as unknown as {pickerOptions:unknown}).pickerOptions=options;return {name:'custom-part.anishape',createWritable:async()=>({write:async(data:string)=>{(window as unknown as {savedCad:string}).savedCad=data},close:async()=>{}})}}})})
 await page.goto('/#projects');await page.getByRole('button',{name:'New project',exact:true}).click();await page.getByRole('dialog').getByRole('button',{name:'Create project',exact:true}).click();await page.getByRole('button',{name:'Create sketch',exact:true}).click();await page.getByRole('button',{name:'Sketch on XY plane',exact:true}).click();await page.getByRole('button',{name:'Finish sketch',exact:true}).click();await page.getByRole('button',{name:'Extrude',exact:true}).click();await page.getByRole('button',{name:'Apply',exact:true}).click();await expect(page.getByTestId('model-volume')).toHaveText('24000.000 mm³');await page.getByRole('button',{name:'Save as .anishape',exact:true}).click();await expect(page.getByText('Saved to custom-part.anishape',{exact:true})).toBeVisible();const data=await page.evaluate(()=>(window as unknown as {savedCad:string}).savedCad);const saved=JSON.parse(data);expect(saved.format).toBe('AnishapeGeometry');expect(saved.history.past.length).toBe(2);expect(await page.evaluate(()=>(window as unknown as {pickerOptions:{suggestedName:string}}).pickerOptions.suggestedName)).toMatch(/\.anishape$/);
 await page.getByRole('button',{name:'Extrude 1',exact:true}).click();await page.getByLabel('Distance',{exact:true}).fill('20');await page.getByRole('button',{name:'Apply',exact:true}).click();await expect(page.getByTestId('model-volume')).toHaveText('30000.000 mm³');await page.getByRole('button',{name:'Save project',exact:true}).click();await expect.poll(()=>page.evaluate(()=>JSON.parse((window as unknown as {savedCad:string}).savedCad).document.features[1].parameters.distance)).toBe(20);
 await page.getByLabel('Open editable CAD file',{exact:true}).setInputFiles({name:'from-custom-folder.anishape',mimeType:'application/json',buffer:Buffer.from(data)});await expect(page.getByTestId('model-volume')).toHaveText('24000.000 mm³');await page.getByRole('button',{name:'Undo',exact:true}).click();await expect(page.getByRole('button',{name:'Extrude 1',exact:true})).toHaveCount(0);await page.getByRole('button',{name:'Redo',exact:true}).click();await expect(page.getByTestId('model-volume')).toHaveText('24000.000 mm³');await page.getByLabel('Open editable CAD file',{exact:true}).setInputFiles({name:'bad.anishape',mimeType:'application/json',buffer:Buffer.from('{}')});await expect(page.getByRole('alert')).toBeVisible();await expect(page.getByTestId('model-volume')).toHaveText('24000.000 mm³')
})
