import {test,expect} from '@playwright/test'
test('finished sketches stay visible, hide after consumption, and can be shown and saved again',async({page},info)=>{
 await page.goto('/#projects');await page.getByRole('button',{name:'New project',exact:true}).click();await page.getByRole('dialog').getByRole('button',{name:'Create project',exact:true}).click();await page.getByRole('button',{name:'Create sketch',exact:true}).click();await page.getByRole('button',{name:'Sketch on XY plane',exact:true}).click();await page.getByRole('button',{name:'Finish sketch',exact:true}).click();
 const viewport=page.locator('.model-viewport-layer .viewport');await expect(viewport).toHaveAttribute('data-visible-sketches','1');await expect(page.getByRole('button',{name:'Hide Sketch 1',exact:true})).toBeVisible();await page.screenshot({path:info.outputPath('finished-sketch.png')});
 await page.getByRole('button',{name:'Extrude',exact:true}).click();await expect(viewport).toHaveAttribute('data-visible-sketches','1');await page.getByRole('button',{name:'Cancel',exact:true}).click();await expect(viewport).toHaveAttribute('data-visible-sketches','1');await page.getByRole('button',{name:'Extrude',exact:true}).click();await page.getByRole('button',{name:'Apply',exact:true}).click();await expect(page.getByTestId('model-volume')).toHaveText('24000.000 mm³');await expect(viewport).toHaveAttribute('data-visible-sketches','0');
 await page.getByRole('button',{name:'Show Sketch 1',exact:true}).click();await expect(viewport).toHaveAttribute('data-visible-sketches','1');await expect(page.getByText('Saved locally',{exact:true})).toBeVisible();await page.reload();await expect(viewport).toHaveAttribute('data-visible-sketches','1');await page.getByRole('button',{name:'Automatic visibility for Sketch 1',exact:true}).click();await expect(viewport).toHaveAttribute('data-visible-sketches','0');await page.getByRole('button',{name:'Undo',exact:true}).click();await expect(viewport).toHaveAttribute('data-visible-sketches','1');
})
